alias venv="source .venv/bin/activate"
# 1: name of conda env, 2: python version
cenv() {
    conda create -n "$1" python="$2"
}

# 1: name of parent conda env
mkvenv() {
    conda activate "$1"
    python -m venv "$(pwd)/.venv"
    "$(pwd)"/.venv/bin/python -m pip install --upgrade pip
    venv
}